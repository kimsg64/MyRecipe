import DefaultLayout from '@layouts/DefaultLayout';

const Record = () => {
    return (
        <DefaultLayout>
            <div>Record</div>
        </DefaultLayout>
    );
};

export default Record;
